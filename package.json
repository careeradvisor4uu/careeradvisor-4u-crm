{
  "dependencies": {
    "node-cron": "^4.2.1",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  }
}
